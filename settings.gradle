// settings.gradle
rootProject.name = "RedirectCameraHook"
include ':app'
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://api.xposed.info/' }
    }
}
