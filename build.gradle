buildscript {
    repositories {
        google()
        mavenCentral()                              # untuk plugin Android 
        maven { url "https://repo.spring.io/libs-milestone" }  # Xposed API stub 
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.4'
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "https://repo.spring.io/libs-milestone" }  # agar compileOnly 'de.robv.android.xposed:api:82' 
    }
}
