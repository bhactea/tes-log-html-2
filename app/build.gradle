apply plugin: 'com.android.application'

android {
    namespace "com.bhakti.redirectcamera"      // **Wajib** untuk AGP ≥ 8.0
    compileSdkVersion 34                       // **Harus** ≥34
    defaultConfig {
        applicationId "com.harpamobilehr"
        minSdkVersion 24
        targetSdkVersion 34                    // **Disarankan** sama dengan compileSdk
    }
    buildTypes {
        release { minifyEnabled false }
    }
}

dependencies {
    compileOnly files('libs/api-82.jar')
    // ... dependency lain ...
    implementation 'androidx.appcompat:appcompat:1.7.0'
}
