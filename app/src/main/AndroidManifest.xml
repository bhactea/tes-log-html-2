<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bhakti.redirectcamera">

<application
    android:name="com.harpamobilehr.MainApplication"
    android:allowBackup="false"
    android:extractNativeLibs="false"
    android:usesCleartextTraffic="false"
    android:networkSecurityConfig="@xml/network_security_config"
    android:appComponentFactory="androidx.core.app.CoreComponentFactory"
    android:requestLegacyExternalStorage="true"
    android:theme="@style/AppTheme">  <!-- theme umum -->
    
    <activity
        android:name="com.harpamobilehr.MainActivity"
        android:exported="true"
        android:launchMode="singleTop"
        android:screenOrientation="portrait"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/AppTheme.Launcher">  <!-- override untuk preview window -->
        
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- ...intent-filter lain... -->
    </activity>
    
    <!-- deklarasi service, receiver, provider lain -->
</application>


</manifest>
