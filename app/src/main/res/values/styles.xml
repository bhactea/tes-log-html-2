<resources>
  <!-- Theme untuk launcher, menonaktifkan preview window -->
  <style name="AppTheme.Launcher" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Hilangkan kilasan putih sebelum onCreate() -->
    <item name="android:windowDisablePreview">true</item>
  </style>

  <!-- Theme utama tetap bisa pake AppTheme yang sudah ada -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- ...item lain jika perlu... -->
  </style>
</resources>
